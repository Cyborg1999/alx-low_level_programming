#include "main.h"
#include <ctype.h>
/**
 * islower - function to find if a character is lowercase
 *
 * Return: Always 0 (Success).
 */
int _islower(int i)
{
	int i;

	if (int i > 96 && i < 123)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}

