#include "main.h"
#include <ctype.h>
/**
 * islower - function to find if a character is lowercase
 *
 * Return: Always 0 (Success).
 */
void _islower(void)
{
	int i;

	if (int i > 96 && i < 123)
	{
		_putchar(1);
	}
	else
	{
		_putchar(0);
	}
}

